import type { Metadata } from 'next';import Link from 'next/link';import { createAdminDatabase,listLectureDrafts } from '@/services/knowledge-drafts';import styles from '@/features/admin/KnowledgeEditor.module.css';export const metadata:Metadata={title:'강의 목록',robots:{index:false,follow:false,nocache:true}};
export default async function Page(){const {pool}=createAdminDatabase();try{const items=await listLectureDrafts(pool);return <main className={styles.shell}><nav className={styles.nav}><Link href="/admin">DECHIVE ADMIN</Link><Link className={`${styles.button} ${styles.primary}`} href="/admin/lectures/new">새 강의</Link></nav><h1 className={styles.title}>강의</h1><p className={styles.lead}>지식을 바탕으로 설명·예시·실습·확인 과정으로 재구성한 학습 자료입니다.</p>{items.length?<div className={styles.tableWrap}><table className={styles.list}><thead><tr><th>제목</th><th>언어</th><th>상태</th><th>Version</th><th></th></tr></thead><tbody>{items.map(item=><tr key={String(item.id)}><td>{String(item.title)}</td><td>{String(item.locale)}</td><td>{String(item.status)}</td><td>{String(item.versionNumber)}</td><td><Link className={styles.textLink} href={`/admin/lectures/${item.id}/edit`}>편집</Link></td></tr>)}</tbody></table></div>:<p className={styles.empty}>강의 Draft가 없습니다.</p>}</main>}finally{await pool.end()}}
