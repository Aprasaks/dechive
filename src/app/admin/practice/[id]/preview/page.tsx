import Link from 'next/link';import { notFound } from 'next/navigation';import { DechiveDocumentRenderer } from '@/features/admin/DechiveDocumentRenderer';import { createPracticeAdminDatabase,getPracticeDraft } from '@/services/practice-drafts';import styles from '@/features/admin/KnowledgeEditor.module.css';export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const {pool}=createPracticeAdminDatabase();try{const draft=await getPracticeDraft(pool,id);if(!draft)notFound();return <main className={styles.shell}><nav className={styles.nav}><Link href={`/admin/practice/${id}/edit`}>← 편집</Link></nav><div className={styles.notice}>현재 Draft 미리보기이며 공개 콘텐츠가 아닙니다.</div><h1 className={styles.title}>{draft.title}</h1><p>{draft.summary}</p><h2>목적</h2><p>{draft.objective}</p><DechiveDocumentRenderer document={draft.document}/></main>}finally{await pool.end()}}
