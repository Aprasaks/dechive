'use server';
import { revalidatePath } from 'next/cache';
import { requireOwnerAction } from '@/features/admin/owner-auth';
import type { PracticeDraftInput } from '@/services/practice-drafts';
import { createPracticeAdminDatabase,createPracticeDraft,publishPracticeDraft,updatePracticeDraft } from '@/services/practice-drafts';
export type PracticeSaveRequest=PracticeDraftInput&{mode:'create'|'edit';localizationId?:string};
export async function savePracticeDraftAction(request:PracticeSaveRequest){const actorId=await requireOwnerAction();const {pool}=createPracticeAdminDatabase();try{const saved=request.mode==='create'?await createPracticeDraft(pool,request,{actorId}):await updatePracticeDraft(pool,request.localizationId??'',request,{actorId});for(const path of ['/admin','/admin/practice',`/admin/practice/${saved.localizationId}/edit`,`/admin/practice/${saved.localizationId}/preview`])revalidatePath(path);return {ok:true as const,localizationId:saved.localizationId,versionNumber:saved.versionNumber}}catch(error){return {ok:false as const,error:error instanceof Error?error.message.split(':')[0]:'save_failed'}}finally{await pool.end()}}
export async function publishPracticeDraftAction(localizationId:string){const actorId=await requireOwnerAction();const {pool}=createPracticeAdminDatabase();try{const published=await publishPracticeDraft(pool,localizationId,{actorId});for(const path of ['/admin','/admin/practice',`/admin/practice/${localizationId}/edit`,`/admin/practice/${localizationId}/preview`,'/practice',`/practice/${published.slug}`])revalidatePath(path);return {ok:true as const,alreadyPublished:published.alreadyPublished}}catch(error){return {ok:false as const,error:error instanceof Error?error.message.split(':')[0]:'publish_failed'}}finally{await pool.end()}}
