import Link from 'next/link';import { createPracticeAdminDatabase,listPracticeDrafts } from '@/services/practice-drafts';import styles from '@/features/admin/KnowledgeEditor.module.css';export default async function Page(){const {pool}=createPracticeAdminDatabase();try{const items=await listPracticeDrafts(pool);return <main className={styles.shell}><nav className={styles.nav}><Link href="/admin">DECHIVE ADMIN</Link><Link className={`${styles.button} ${styles.primary}`} href="/admin/practice/new">새 Practice</Link></nav><h1 className={styles.title}>Practice</h1><p className={styles.lead}>지식을 직접 적용하고 관찰·검증한 기록을 관리합니다.</p>{items.length?<div className={styles.tableWrap}><table className={styles.list}><tbody>{items.map(item=><tr key={String(item.id)}><td>{String(item.title)}</td><td>{String(item.status)}</td><td><Link href={`/admin/practice/${item.id}/edit`}>편집</Link></td></tr>)}</tbody></table></div>:<p className={styles.empty}>Practice Draft가 없습니다.</p>}</main>}finally{await pool.end()}}
