# AGENTS.md

## 1. Read First

This repository uses Next.js 16 with the App Router. If behavior looks unfamiliar, check the local docs under `node_modules/next/dist/docs/` before guessing.

Before any change:

1. Run `git status`.
2. Identify existing user, Codex, or Claude Code changes.
3. Do not overwrite changes from another agent.
4. If ownership is unclear, stop and report.

## 2. Tech Stack

From `package.json` (as of 2026-07):

```txt
Next.js 16 (App Router)     next ^16.2.3
React 19                    react 19.2.4
TypeScript 5                admin area is legacy JS/JSX; write new code in TS
Tailwind CSS 4              @tailwindcss/postcss, @tailwindcss/typography,
                            tw-animate-css, shadcn/tailwind.css imported in globals.css
Markdown                    gray-matter, react-markdown, remark-gfm,
                            rehype-highlight, rehype-slug
Icons                       lucide-react
Guestbook storage           @supabase/supabase-js
Admin analytics             @google-analytics/data (GA4 Data API), bcryptjs
Scripts runner              tsx
Translation                 @anthropic-ai/sdk (devDep — used by the content
                            submodule's translate.mjs, resolved from root node_modules)
Deploy                      Vercel (auto-deploy on push to main)
```

Do not remove `shadcn` (used via `@import "shadcn/tailwind.css"` in `globals.css`) or `@anthropic-ai/sdk` (used by `npm run translate`) even though neither is imported from `src/`.

## 3. Routes

```txt
/                          Home. Desktop (md+) renders DailyIssueCover (daily cover);
                           mobile renders MobileHomeCover (4-slide horizontal swipe)
/archive[/[slug]]          Archive index/detail (KO)
/en/archive[/[slug]]       Archive index/detail (EN)
/deep-dive[/[slug]]        Deep Dive landing/detail (KO)
/en/deep-dive[/[slug]]     Deep Dive landing/detail (EN)
/book[/[slug]]             Book notes (KO source only, loader supports .en.md)
/ai-updates[/[date]]       AI update briefings
/issues                    Daily cover album (responsive grid)
/issues/[date]             Daily cover detail + Issue Navigator
/guestbook /about /privacy-policy   Shared URLs; UI copy switches via language state
/admin/login /admin/analytics       Admin UI (JSX)
/api/admin/{login,logout,analytics} Admin API — also called by an external dashboard
/api/guestbook             Guestbook API (Supabase)
/feed.xml /en/feed.xml /sitemap.xml /robots.txt
```

### Daily Issue cover system

- Data: manual entries in `src/data/dailyIssues.ts`; `src/lib/dailyIssues.ts` auto-generates covers from Archive posts newer than the latest configured entry.
- Three cover templates, rotated by day-of-month % 3: `classic-editorial`, `big-question`, `side-cover-lines`.
- Template cover images live in `public/images/covers/YYYY-MM/template-*.webp` (per month); fallback is `/images/bg.webp`.
- Covers combine `weeklyEditions` (weekly book) and `dailyAiUpdates` data.
- Issue Navigator on `/issues/[date]`: previous / all / next. Desktop: right floating panel; mobile: fixed bottom bar.

### KO/EN routing

- `/en/...` routes exist only for archive and deep-dive.
- Other pages keep shared URLs and switch copy via `LangProvider` state (`src/lib/i18n.ts` dictionaries).
- Preserve the selected language across navigation; never collapse EN content links back to KO URLs.

## 4. Directory Structure

```txt
src/app/                 App Router routes (pages are server components)
src/components/          Feature folders: about, ai-updates, analytics, archive,
                         book, deep-dive, guestbook, home, issues, layout, privacy
src/lib/                 Loaders/utils: posts.ts, books.ts, dailyIssues.ts, i18n.ts,
                         rss.ts, plus admin-only JS (adminAuth, ga4Client, analytics*)
src/data/                TS constant data: dailyIssues, dailyAiUpdates,
                         weeklyEditions, aiUpdates
src/types/               Shared types
src/content/books/       Book note markdown (.ko.md) — main repo, not the submodule
content/                 Content submodule (Aprasaks/dechive-content): posts/*.ko.md + .en.md
scripts/                 check-content.ts, sync-images.ts, translate.ts (wrapper)
public/images/covers/    Monthly cover templates (YYYY-MM/template-*.webp)
public/images/books/     Weekly book covers
public/images/posts/     Generated by sync-images.ts — gitignored, never edit directly
public/audio/            background-music1~9.mp3 (MusicProvider builds filenames
                         dynamically; all nine are required)
```

## 5. Conventions

- Page pattern: `page.tsx` is a server component handling metadata and data loading; interactive UI lives in `*Client.tsx` (`'use client'`) inside the matching feature folder.
- Import alias `@/*` → `src/*`.
- Styling: inline Tailwind utility classes; `globals.css` is the only global stylesheet. Palette: warm ivory `#f8f6f1`, dark `#080806`, muted gold `#d6a352`; thin borders, wide margins, editorial magazine feel.
- Fonts via `next/font/google` (Geist, Noto Serif KR, …) exposed as `--font-*` CSS variables.
- Naming: kebab-case route folders, PascalCase components, camelCase lib/data modules.
- Content filenames: `slug.ko.md` / `slug.en.md`. Korean is the source language.

## 6. Commands

```bash
npm run dev            # sync-images then next dev
npm run build          # check-content + sync-images + next build
npm run lint           # eslint
npm run check:content  # frontmatter validation only
npm run translate      # create missing .en.md only (protects existing en.md)
npm run translate -- --force   # forced retranslation, requires review afterwards
npx tsc --noEmit       # type check
```

Deploy: push to `main` → Vercel auto-deploys.
Env vars (managed in Vercel): `ADMIN_ID`, `ADMIN_PASSWORD`, `ADMIN_SESSION_SECRET`, `GA_CLIENT_EMAIL`, `GA_PRIVATE_KEY`, `GA_PROPERTY_ID`, `SUPABASE_URL`, `SUPABASE_ANON_KEY`.

## 7. Cautions / Do Not Touch

- **The admin API is used by an external dashboard. Never delete or modify it.** This covers `/api/admin/login|logout|analytics`, `src/lib/adminAuth.js`, `src/lib/ga4Client.js`, `src/lib/analyticsDateRange.js`, `src/lib/analyticsInsight.js`, `src/lib/analyticsLabels.js`, and the `@google-analytics/data` and `bcryptjs` dependencies.
- Mobile is not implemented for the daily cover:
  - `DailyIssueCover` is desktop-only (`hidden md:grid`). The mobile home shows `MobileHomeCover` instead.
  - On `/issues/[date]`, mobile renders only the bottom navigation bar; the cover body itself does not appear below `md`.
- There is no concept-index ("Index") route yet; `concepts` exists only as frontmatter.
- The request-post API and Discord webhook integration were removed in the 2026-07-02 cleanup.
- Without an explicit request, do not touch: KO/EN dual routing, sitemap, Google Analytics, AdSense (`public/ads.txt`, adsbygoogle script in layout), privacy policy, Vercel env vars, translation scripts, the content submodule workflow (`translate.yml`).

## 8. Agent Ownership

Current Dechive identity:

- Core line: `AI는 답을 만든다. Dechive는 그 답을 검증한다.`
- Dechive is a personal archive that turns AI-era answers into verifiable records.
- Dechive language should center verification, questions, records, criteria, and judgment.
- Avoid retired book-place metaphors unless the user explicitly asks to restore them.
- Current markdown content types are `archive` and `deepdive`; book notes live in `src/content/books`.
- `daily` is not a markdown content type. The Daily Issue cover system is code-level data/UI, not content.

Codex owns:

- application code
- routes and components
- refactors
- tests
- type/lint/build verification
- app code that consumes content

Claude Code owns:

- content creation and editing
- copywriting
- planning and review
- translation script runs
- content submodule commits when requested

Shared only by explicit user request:

- workflow files
- translation scripts
- content metadata cleanup
- content submodule pointer updates

Codex must not edit `content/` or run translation unless explicitly asked.

## 9. Content Submodule Workflow

`content/` is not a normal folder. It is a Git submodule pointing to:

```txt
Aprasaks/dechive-content
```

Posts are written under:

```txt
content/posts/*.ko.md
```

But the actual content source repository is `dechive-content`.

When editing posts:

1. Modify files under `content/posts`.
2. Commit and push inside `content/` first.
3. Return to the root repository.
4. Commit and push the updated `content` submodule pointer in the root repository.

Never commit only the root submodule pointer before committing the content repository.

Note: book notes under `src/content/books/` belong to the main repo and follow the normal root-repo commit flow.

## 10. Harness Rules

Harness engineering means binding AI work to:

- goal
- scope
- boundary
- verification
- stop condition

Before editing, clarify internally:

- What problem is this change solving?
- Which files are allowed to change?
- Which systems must not be touched?
- What check proves the change is safe?
- When should the agent stop instead of guessing?

Stop and report when:

- git status shows unexpected changes
- the task crosses agent ownership boundaries
- the fix requires unrelated infrastructure changes
- errors appear unrelated to the current task
- the request conflicts with Dechive identity

A good harness makes the agent narrower, not louder.

## 11. Content Type Rules

Current content types:

- `archive`: an independent record built around one question
- `deepdive`: a long-form knowledge document that carries one deep question through concepts, examples, mistakes, limits, reasoning, and verification criteria
- book notes: reading records in `src/content/books` with their own frontmatter (title, author, readDate, coverImage, summary)

Rules:

- Do not add `series`.
- Do not add previous/next/series flows for Archive content.
- Archive posts must be readable independently.
- Deep Dive documents should preserve clear structure for future AI reasoning and verification guidance.
- Frontmatter is validated by `scripts/check-content.ts` at build time. Common required fields: `title, seoTitle, date, type, category, tags, slug, description, status, lang`. Archive must not use `subject/coverImage/concepts/updated/thumbnail`; Deep Dive requires `coverImage` and `concepts` and must not use `subject/updated/thumbnail`.

## 12. Language Routing Rules

Dechive uses Korean as the default source language and English as a protected translated content layer.

Rules:

- Content index/detail routes with separate language content have language-specific URLs:
  - `/archive`, `/en/archive`, `/archive/[slug]`, `/en/archive/[slug]`
  - `/deep-dive`, `/en/deep-dive`, `/deep-dive/[slug]`, `/en/deep-dive/[slug]`
- Shared utility or static pages do not get `/en/...` routes unless explicitly requested. Keep `/guestbook`, `/about`, `/privacy-policy`, `/book`, `/ai-updates`, and `/issues` as shared URLs; render their UI copy from persisted language state.
- When a visitor selects KO or EN, preserve that language across navigation.
- Header, home, list, and detail links must generate language-appropriate content URLs.
- Do not collapse English content navigation back to Korean URLs.

## 13. Translation Policy

Korean posts are the source.  
Existing English posts are protected editorial content.

Canonical translation logic lives in:

```txt
content/.github/scripts/translate.mjs
```

Root script `scripts/translate.ts` is only a wrapper for `npm run translate`. Do not reintroduce translation prompts, frontmatter reconstruction, Anthropic calls, or file translation logic into the root wrapper.

Default:

- create missing `.en.md` files only
- skip existing `.en.md`
- do not overwrite reviewed English content

Force retranslation only when explicitly requested:

- GitHub Actions: `force_retranslate: true`
- Local: `npm run translate -- --force`

New English posts should translate: `title`, `description`, `seoTitle`, `tags`, body, and `lang: ko → en`.

Do not translate: `slug`, `type`, `status`, `date`, `category`, `concepts`.

## 14. Commit / Push

Do not commit or push unless explicitly asked.

- "커밋해" = commit only
- "푸시해" = push only
- "커밋푸시해" = commit and push

When content submodule changes are involved:

1. Commit/push inside `content/` first.
2. Commit/push the root submodule pointer second.

## 15. Report Format

After work, report:

```txt
변경 파일:
- ...

주요 변경:
- ...

확인:
- ...

주의/TODO:
- ...
```
